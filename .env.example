# 이 파일은 예시입니다. 실제 값은 이 파일을 복사해서 .env로 만들고 채우세요.
# .env는 .gitignore에 포함되어 있어서 깃허브에 올라가지 않습니다.
# (이 .env는 서버 코드를 손으로 직접 실행해서 테스트할 때만 필요합니다.
#  Claude Desktop/Code/웹이 이 MCP를 실행할 때는 이 파일을 읽지 않고,
#  아래 "Claude Desktop 등록" 안내처럼 설정 파일의 env 항목에 키를 직접 넣습니다.)

SEOUL_OPENAPI_KEY=<YOUR_SEOUL_OPENAPI_KEY>
