# CLAUDE.md — 서울시 건설알림이 MCP 프로젝트

## 프로젝트 개요
서울시 열린데이터광장의 건설공사 관련 데이터를 MCP(Model Context Protocol) 서버로
제공하는 프로젝트. Claude Desktop/Code에 로컬로 등록해 자연어로 서울시 공사 현황을
검색/조회할 수 있게 해준다.

- 배포 방식: **로컬 전용(stdio)**. 클라우드 서버나 URL이 없다 — 각자 이 저장소를
  clone해 자기 컴퓨터에서 Claude가 직접 프로세스를 실행하는 방식이다. (과거
  fly.io 웹 배포 버전은 2026-09-03에 완전히 삭제됐고, 2026-09-08에 stdio 구조로
  전환하며 관련 코드/설정도 모두 제거함. 배경은 DEVLOG.md "2026-09-07~08" 항목
  참고.)
- GitHub: hlucent/seoul-construction-mcp
- **실제 작업 폴더(확정): C:\Users\<집PC계정명>\Projects\seoul-construction-mcp**
  (집 PC) / **C:\Users\<사무실PC계정명>\Projects\seoul-construction-mcp** (사무실 PC)
  (.git 존재, origin이 github.com/hlucent/seoul-construction-mcp로 연결됨,
  최신 커밋과 코드가 일치하는 진짜 작업 폴더)
- 실제 코드 진입점: src/index.js (Node.js 프로젝트, 단일 파일. main.py 아님)
- 참고: 과거 저장소명은 construction-alert-mcp였으나 이후 seoul-construction-mcp로
  개편됨(package.json의 name, index.js의 serverInfo.name 모두 통일 완료). 앞으로
  이 프로젝트 작업은 반드시 위 Projects 폴더에서만 진행할 것.
- 사용자(청정)는 비개발자 — 모든 안내는 복사-붙여넣기 가능한 완성된 명령어로,
  어느 창(PowerShell/메모장/브라우저)에 입력하는지 명확히 구분해서 제공할 것.

## 현재 제공 중인 도구 (4개)
1. search_construction_projects — 사업개요 검색 (OA-15585), 자치구/키워드/도급액 필터
   + 발주처_연락처/건설사업관리단_연락처/시공사_연락처(TEL_1~3) 필드 포함
2. search_construction_work_by_district — 자치구별 공사 정보 검색 (OA-1222)
3. get_construction_progress — 공정률/기성률/D-Day 등 진행 현황.
   공정률/대비율이 0이면 "미입력"으로 표시(formatRate 헬퍼). min_amount(도급액 기준)
   클라이언트단 필터 지원. API 자체 필터는 없음.
4. get_construction_project_photos — 사업코드(pjt_cd)로 현장 사진 목록 조회 (OA-15586)
   - 모든 사업에 사진이 있는 건 아님 (소규모/단기 사업은 사진 없는 경우 많음)
   - 대형 장기사업(예: 강남순환도시고속도로)은 주기적으로 사진 업데이트됨

## 아키텍처 핵심 사항
- **stdio 전송 방식**: `StdioServerTransport` 사용. Claude Desktop/Code가 node
  프로세스를 직접 실행해 stdin/stdout으로 통신한다. HTTP 서버, express, 인증
  (`?key=`), rate limit, 별도 프록시(mcp-remote 등)가 전부 없다 — 이 프로세스
  자체가 실행하는 컴퓨터 안에서만 동작하므로 외부에서 접근할 방법이 없어
  인증이 불필요하다.
- `SEOUL_OPENAPI_KEY`(서울시 업스트림 API 호출용)만 필요한 유일한 키. Claude
  Desktop/Code 설정 파일의 `env` 항목에 직접 넣거나, 로컬 수동 테스트 시에는
  `.env` 파일로 넣는다(`dotenv/config`로 로드).
- CDATA XML 파싱 이슈 수정 완료
- Dockerfile, deploy.ps1(과거 fly.io 배포용)은 2026-09-08 stdio 전환 시 저장소에서
  삭제함(git 히스토리에는 남아있음).
- 참고: 로컬 실행 시 필요한 유일한 npm 스크립트는 `npm start`(=`node src/index.js`).
- Claude Desktop 등록 시 주의점 (실제로 겪은 트러블, DEVLOG 2026-09-07~08 참고):
  - Microsoft Store(MSIX) 버전은 `%APPDATA%\Claude\claude_desktop_config.json`이
    아니라 `%LOCALAPPDATA%\Packages\Claude_<임의문자열>\LocalCache\Roaming\Claude\claude_desktop_config.json`이
    실제 설정 파일이다. 겉보기 표준 경로를 아무리 고쳐도 반영 안 되면 이 경로부터
    확인할 것.
  - `"command": "node"`보다 `"command"`를 node.exe 전체 경로(예:
    `C:\\Program Files\\nodejs\\node.exe`, PowerShell의
    `(Get-Command node).Source`로 확인)로 지정하는 쪽이 더 안정적으로 연결됨.

## 요청 작성 기본 틀 — 접수표 5칸
Claude Code에게 새 작업을 요청할 때는 항상 아래 5가지를 채워서 지시할 것.
챗봇처럼 "질문 → 답변 복붙"이 아니라, 에이전트에게 "역할을 부여해 대행시키는" 방식으로 접근한다.

1. **역할** — 예: "서울시 건설알림이 MCP 서버를 관리하는 백엔드 개발자로서 일해줘"
2. **상황** — 예: "지금 get_construction_progress 도구는 자치구·최소금액 필터가
   클라이언트단에서만 처리되고 있어"
3. **문제** — 예: "API 자체 필터가 없어서 전체 데이터를 받아온 뒤 걸러내다 보니
   토큰을 많이 쓰고 있어"
4. **결과물** — 예: "필터링 로직을 개선한 새 버전을 만들어줘"
5. **기준** — 예: "본 프로젝트 폴더 밖의 파일은 절대 건드리지 말고, 기존 src/index.js는
   지우거나 통째로 덮어쓰지 마. 큰 구조 변경이 필요하면 진행 전에 반드시 나에게 먼저 물어봐."

→ 디테일하게 채울수록(특히 상황·문제) Claude Code가 만드는 계획의 정확도가 올라간다.
   서울시 데이터의 도메인 지식(예: "소규모 사업은 사진이 없을 수 있다", "OA-1222는
   자치구별로만 조회된다" 등)은 AI가 아니라 내가 먼저 알고 있어야 정확히 지시할 수 있다.

## 문제 분할 원칙
- Claude Code에게 한 번에 여러 문제를 동시에 요청하지 않는다.
  (예: "새 도구도 추가하고, 필터링도 고치고, 문서도 정리해줘" ❌)
- 하나의 요청에는 하나의 문제만 담는다.
  (예: "get_construction_progress에 자치구 필터 로직만 고쳐줘" ✅)
- 문제가 크다고 느껴지면(여러 파일·여러 단계에 걸침) 먼저 "이 작업을 몇 단계로
  쪼갤 수 있을지 계획부터 세워줘"라고 요청한 뒤, 계획서를 보고 단계별로 하나씩 진행한다.

## 반복 작업의 스킬(Skill)화 원칙
- 같은 유형의 요청이 3회 이상 반복된다면, 매번 처음부터 설명하지 말고
  **스킬 파일**로 고정해 토큰을 절약한다.
- 이 프로젝트에서 스킬화 후보:
  - 새 서울시 열린데이터 API를 도구로 추가하는 절차 (엔드포인트 등록 →
    파싱 로직 → CDATA 이슈 처리 → 테스트 → 커밋 메시지 규칙까지 일련의 절차)
  - Claude Desktop에 신규 컴퓨터를 등록하는 절차 (config 경로 확인 →
    Microsoft Store 버전 여부 판별 → node 전체 경로 확인 → 등록 → 완전종료 후
    재확인)

## 결과물 방향이 애매할 때 — 후보 병렬 요청법
- 어떤 형식/구조로 결과물을 받아야 할지 스스로도 확신이 없을 때는,
  바로 전체 작업을 시키지 말고 먼저 다음처럼 요청한다:
  - "이 문제를 해결할 수 있는 접근법 3가지를 (구현하지 말고) 개요만 먼저 보여줘"
  - "필터링 로직 설계 방향을 후보 2~3가지로 요약해줘"
- 후보를 먼저 확인한 뒤 하나를 선택해 본작업을 지시하면, 잘못된 방향으로
  전체를 진행시켜 재작업(=토큰 낭비)하는 상황을 예방할 수 있다.

## 작업 범위 제한 원칙
- Claude Code에게 지시할 때는 반드시 아래 두 문구를 포함할 것:
  - "이 프로젝트 폴더 안에서만 작업해줘"
  - "기존 파일을 지우거나 통째로 덮어쓰지 마"
- 큰 결정(아키텍처 변경, 새 데이터셋 통합, 삭제성 작업 등)은
  "큰 결정은 반드시 나에게 먼저 물어보고 진행해줘"라는 기준을 요청 시 명시할 것.
- **파일/폴더 삭제처럼 되돌리기 어려운 작업은, Claude Code가 "삭제해도 되는지
  먼저 물어보겠다"고 말해놓고 실제로는 먼저 실행해버리는 경우가 있었다
  (2026-08-05, Downloads 사본 삭제 건 — 다행히 안전한 사본이라 문제는 없었음).
  삭제·이름변경·덮어쓰기 등은 "물어보겠다"는 말만으로 안심하지 말고, 실제로
  실행 여부를 다시 확인할 것.**

## 자동 검증 원칙
- Claude Code가 "완료했습니다"라고 보고할 때, 가능하면 아래처럼
  **AI가 스스로 실제 동작을 확인**하도록 요청할 것:
  - 새 도구/기능이면: "stdin으로 직접 JSON-RPC 요청을 넣어보고 정상 응답 오는지
    확인해줘" (예: `echo '{...initialize...}' | node src/index.js`)
  - 실데이터 조회 도구면: "실제 서울시 API를 호출해서 진짜 데이터가 나오는지까지
    확인해줘" (가짜/모의 데이터로 채우지 않았는지 확인)
  - 삭제/수정 기능이면: "실제로 삭제(또는 수정)가 반영되는지까지 확인하고 보고해줘"

## 결과 보고 시 3대 검증 체크리스트
Claude Code가 "완료했습니다"라고 보고하면, 다음 3가지를 반드시 순서대로 확인한다.

1. **사실인가?** — API 응답이 실제 데이터인지, 그럴듯한 모의(가짜) 데이터로
   채워 넣은 건 아닌지 확인한다. (예: 사업 사진이 없는데 임의로 링크를
   만들어내지 않았는지)
2. **요청한 것이 다 반영됐는가?** — 처음 지시한 내용 중 빠진 부분이 없는지
   원본 요청과 결과물을 하나씩 대조한다.
3. **경계선(기준)을 지켰는가?** — "지킬 것/피할 것"으로 명시했던 기준
   (예: src/index.js 통째로 덮어쓰기 금지, 큰 결정 사전 확인 등)을 실제로
   지켰는지 확인한다.

이 3가지를 확인하지 않은 채 "다음 작업"으로 넘어가지 않는다.

## 데이터 정확성 원칙 (숫자·수치 관련)
- API에서 반환된 수치(공정률, 기성률, 도급액 등)는 **반드시 원본 응답값만 그대로 표시**할 것.
- 값이 없거나 API가 필터를 지원하지 않는 항목(예: get_construction_progress의 자치구·최소금액)을
  **추정하거나 임의로 보정해서 채우지 말 것**. 없으면 "해당 데이터 없음"으로 명시.
- 클라이언트단 필터링(예: 최소금액 필터)을 적용한 경우, 결과에 "필터 적용됨"이라고 반드시 표기.

## 민감정보 처리 원칙
- 실명, 전화번호, 소속·부서명 등 특정 개인이나 조직을 식별할 수 있는 정보는
  **파일이나 커밋 메시지에 어떤 형태로도 남기지 말 것**. 직접 노출뿐 아니라
  간접 노출도 금지 대상이다 — 예를 들어 특정 직군·조직에서만 쓰는 관용 표현
  ("담당자", "기안", "결재" 등)을 프로젝트 성격과 함께 쓰면 저장소 주인의
  신원이 간접적으로 추정될 수 있으므로, 이런 표현도 일반적인 단어(예: "연락처",
  "작성자")로 바꿔 쓴다.
- API 응답 필드를 설명할 때는 예외로 둔다 — 예를 들어 "시공사/감리사/발주처
  담당자"처럼 공사 현장 데이터 자체에 있는 필드명을 그대로 옮겨 적는 것은
  이 저장소 소유자의 신원과 무관하므로 문제 없다. 구분 기준은 "그 표현이
  가리키는 대상이 이 프로젝트를 관리하는 사람 본인인가, 아니면 데이터 안의
  제3의 대상인가"이다.
- `SEOUL_OPENAPI_KEY` 값 자체도 커밋·로그·문서에 노출하지 말 것 — 항상 플레이스홀더로만 표기.

## 결과물 완성 기준 (합격 조건)
- "완료했습니다"는 선언일 뿐 실제 완성이 아님 — 아래 조건을 모두 만족해야 "완료"로 인정:
  1. stdin으로 직접 JSON-RPC(`initialize`, `tools/list` 등) 요청을 넣어 정상
     응답 확인
  2. Claude Desktop/Code 재연결 후 도구가 정상 노출되고, 실제 호출 시 진짜
     서울시 데이터가 반환되는지 확인
  3. 커밋 메시지가 "구체적으로" 작성되었는지 확인 (예: "수정", "fix" 금지)
- 위 3가지를 확인하지 않은 상태에서 "다음 작업으로 넘어가자"고 하지 말 것.

## 보고 및 확인 원칙
- Claude Code가 "다 했습니다"라고 보고하면, **곧바로 다음 지시를 내리지 말고 반드시 관련 파일을 열어
  직접 확인**한 뒤 다음 단계로 진행할 것.
  (예: src/index.js 수정 후 → 실제 파일 내용 확인 / Desktop 재연결 후 → 도구 목록·실제 응답 확인)

## 위험 작업 사전 차단 원칙 (일반)
- 아래 작업은 사전에 명시적 확인 없이는 절대 진행하지 않는다:
  - API 키, 인증 정보 등을 로그/커밋/문서에 노출하는 행위
  - src/index.js 등 핵심 파일의 "삭제" 또는 "통째로 덮어쓰기"
  - 폴더/파일 삭제, 이름 변경 등 되돌리기 어려운 작업
  - .env, Claude Desktop 설정 파일 등의 임의 변경
- "교체"라는 단어가 Claude Code의 계획서에 나오면 반드시 재확인.
- "먼저 확인해보겠다"고 말한 뒤 실제로는 먼저 실행해버리는 경우가 있었으므로,
  삭제·이름변경류 작업은 결과 메시지를 반드시 다시 확인할 것.

## 작업 시작 전 Mode·Model·Effort 세팅

Claude Code로 작업을 시작하기 전, 항상 아래 세 가지를 먼저 정하고 들어갈 것.

### Mode (얼마나 자율적으로 시킬지)
- **오타 수정 등 사소한 패치** → Auto 모드 또는 Accept Edits 모드로 진행 가능
- **새 도구 추가, 큰 로직 변경** → 반드시 Plan 모드(Shift+Tab)로 먼저 계획서를
  받고, 승인 전에 아래 3가지를 체크한 뒤 승인할 것:
  1. **동사** — 뭘 하겠다는 건지 (추가/수정/삭제/교체 등)
  2. **숫자** — 도구 몇 개, 파일 몇 개, 필드 몇 개를 건드리는지
  3. **저장 위치** — src/index.js를 직접 건드리는지, 어느 파일까지 영향이 가는지

### Model (누구에게 맡길지)
- 사용 가능 모델(Claude Code 화면 `/model`에서 실제 확인됨): Opus 5(기본값),
  Fable 5(사용 크레딧 필요), Sonnet 5, Haiku 4.5
- **새 도구 설계, 아키텍처 변경, 전체 구조 판단** → Opus 5 권장
- **반복적인 구현, 오타 수정, 필드 추가 같은 실무 작업** → Sonnet 5로 충분
- 모델 확인/변경은 Claude Code에서 `/model` 입력

### Effort (얼마나 깊이 생각시킬지)
- 사용법(Claude Code 화면에서 실제 확인됨): `/effort <low|medium|high|xhigh|max|auto>`
- 기본값은 high. 토큰이 부족한 주간에는 **medium으로 낮춰서** 작업량 조절
- 복잡한 설계(예: 새 데이터셋 통합, 필터링 로직 재설계)가 필요할 때만
  xhigh 또는 max로 올릴 것
- 단순 확인/조회성 작업(예: git status 확인)은 low로도 충분할 수 있음
- auto 옵션도 있음 (Claude가 작업 난이도에 맞게 자동 조절하는 것으로 추정 —
  실제로 몇 번 써보고 어떻게 동작하는지 확인 후 이 문서에 보완할 것)

## 새 도구 추가 / 큰 로직 변경 시 주의
main 브랜치가 곧 실제 사용 중인 상태이므로, 실험적이거나 리스크가 있는
작업은 계획(Plan 모드)을 먼저 검토한 뒤 진행할 것.

## 문제 발생 시 롤백
- 문제가 생겼는데 원인을 모를 때, "고쳐줘"를 반복하지 말 것
  (고칠수록 코드가 꼬여서 더 악화될 수 있음)
- 대신 아래처럼 직전 정상 커밋으로 되돌리도록 지시:
  "직전에 정상 동작했던 커밋으로 되돌려줘"
  또는 "OO 기능 추가 이전 커밋으로 롤백해줘"
- git log로 커밋 히스토리 확인 → git reset 또는 git revert로 복구
- 이것이 가능하려면 평소 커밋 메시지를 구체적으로 남기는 습관이 필수
- 롤백처럼 반복 시도가 필요한 디버깅 작업은 토큰을 많이 쓰므로,
  이럴 때는 Effort를 medium으로 낮춰 시도 횟수를 늘리는 것도 방법

## 커밋 메시지 원칙
- 항상 무엇을 했는지 구체적으로 작성
- 좋은 예: "get_construction_project_photos 사진 없는 사업 예외처리 추가"
- 나쁜 예: "수정", "업데이트", "fix"
- 구체적인 메시지가 있어야 나중에 "OO 기능 추가 전 커밋으로 되돌려줘" 같은
  정확한 롤백 지시가 가능함

## 참고 데이터 출처
- OA-1222: ListConstructionWorkService (자치구별 공사 정보)
- OA-2540 / OA-15585: ListOnePMISBizInfo / 사업개요 (One-PMIS)
- OA-15586: 건설알림이 공사사진

## 향후 계획
- 서울시 열린데이터 추가 데이터셋 신규 도구화 검토 중
- 이 CLAUDE.md는 GitHub 저장소(hlucent/seoul-construction-mcp)에 커밋되어
  있어 PC 포맷 시에도 유실되지 않음 (전역 CLAUDE.md는 로컬 전용이라 포맷 시
  사라짐 — 프로젝트별 CLAUDE.md는 반드시 저장소에 커밋해 별도 보관)
- 새 데이터셋 통합 시 한 번에 통째로 요청하지 않고, 위 "문제 분할 원칙"에 따라
  작은 단위로 쪼개서 요청
- 반복되는 API 통합 절차는 위 "스킬(Skill)화 원칙"에 따라 스킬 파일로 정리해
  토큰 사용량 관리
- 집 PC에도 이번 사무실 PC와 같은 stdio 방식으로 등록 예정
